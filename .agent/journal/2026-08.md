# 2026-08 Project Journal

> Entries are newest first. Preserve the meaning of existing entries; add a new
> correction for material changes, while allowing meaning-preserving fixes.

<!-- journal-entries -->

## 2026-08-08T21:52:08+08:00 · high · 纳入本机 Nature 学术技能套件

Portable profile 新增显式调用的 `$nature` 路由器及 19 个本机 Nature child skills，使该套学术检索、写作、审稿、返修、绘图、统计、数据与提案工作流可由 profile sync 部署并由个人 GitHub 保留。

- **改动**：child snapshot 对齐 `yuan1z0825/nature-skills` 的 `703c150`，保留本机 frontmatter 与 Codex metadata 增量；补齐 portable UI metadata，全部设置 `allow_implicit_invocation: false`，并记录 Apache-2.0 来源、导入范围、可执行面与更新方法。
- **证据**：20 个 skills 通过 `quick_validate.py`，22 项 repository tests 与集成自有文件的 whitespace check 通过；全量 staged check 仅报告 `703c150` 快照原有的 CRLF/尾随空格，未为此改写第三方正文。Profile preview 为 `ADD 689 / CHANGE 10 / DELETE 0`，其中 Nature suite 新增 610 个 leaves、更新 router 2 个 leaves。
- **影响 / 后续**：当前条目只记录已验证的源码状态；Git commit、active profile apply 与 push 由本任务后续实际结果分别证明。

## 2026-08-08T16:53:04+08:00 · high · 纳入 Matt Pocock Skills v1.2.3

Portable profile 新增 Matt Pocock `v1.2.3` 的 25 个 promoted skills，使新主机可由既有 profile sync 直接部署同一套稳定工程技能。

- **改动**：技能树保持上游原样，manifest 逐项管理；新增上游版本与 MIT 许可证记录，并让结构契约区分本地 `personal-*` 默认提示与第三方 metadata，同时验证手动技能的 Codex 隐式调用禁用策略。
- **证据**：所有导入树与官方标签逐目录一致；22 项 repository tests 与 `git diff --check` 通过；profile preview 未显示这些新技能的 drift，仅显示既有 `personal-monitor-external-jobs` drift。
- **影响 / 后续**：Codex 通用 `quick_validate.py` 不接受上游保留的 `disable-model-invocation` 字段，兼容性由随附 `agents/openai.yaml` 与 repository contract test 覆盖；本次不部署 live profile。

## 2026-08-06T20:46:15+08:00 · medium · 同步 GitHub profile 更新到本机

将 GitHub `main@ff38320` 的 portable profile 应用到当前 Mac；preview 的唯一 managed drift 是 `personal-profile-sync`，monitor skill 已经一致。

- **证据**：`profile_sync.py apply` post-check clean，随后 fresh `check` 返回 `No changes`；仓库与 `origin/main` 在应用时一致。
- **影响 / 后续**：本次只更新一个 managed skill，未修改 automation、host-local 状态或其他 unmanaged profile 内容；apply 备份由 profile sync 保留。

## 2026-08-06T20:43:15+08:00 · high · 收紧长任务监控注册拓扑与模型

外部长任务监控改用最短的产品原生注册路径：本地 owner 直接注册 Scheduled，远端 owner 只创建一个本地 setup task，不再建立同主机 coordinator 或 relay task。

- **改动**：setup、Scheduled 与获授权 live fallback 优先使用 `gpt-5.6-luna` 和 low reasoning；Luna 不可用时仅回退目标默认模型，默认值无法可靠表达则阻塞。
- **证据**：monitor skill 校验通过，22 项 repository tests 与 `git diff --check` 通过；profile preview 未显示该 skill drift，仅显示另一个已合入远端但尚未部署的 managed skill。
- **影响 / 后续**：本次不创建或更新任何 automation；现有监控的关闭状态保持不变。

## 2026-08-06T20:08:24+08:00 · high · 默认工作流改为事件驱动

Portable profile 已把普通工程的验证停止条件、常规内容摘要禁令、低概率防御约束和非机械 rubric 写入默认规则；JOURNAL 改为只记录持久事件，新 planning 计划改用 `task_plan.md` 与 `findings.md` 两个当前状态 owner。

- **改动**：同步更新 portable `AGENTS.md`、两个 skills、JOURNAL 模板、本仓库说明和结构契约测试；既有 `progress.md` 仅作为兼容 legacy，历史条目改为保护语义而非逐字节冻结。
- **证据**：两个 skills 校验通过，21 项 repository tests 全过，whitespace check 通过；profile preview 只显示 portable `AGENTS.md` 与两个相关 skills 的预期 managed drift，未涉及 Hook 或 config。
- **影响 / 后续**：普通解释、浏览和未改变结论的验证不再写 JOURNAL；截至本条写入时，源码改动尚未提交、部署或推送。

## 2026-08-06T17:38:18+08:00 · high · 降低默认工作流的过度防御

- **目标**：讨论 portable profile 是否把安全性、一致性、低概率异常和 rubric 看得过重，并明确禁止把普通个人工程做成安全攻防式审计。
- **结果**：确认核心缺口是现行规则只有验证下限、没有验证上限和停止条件；建议在 portable `AGENTS.md` 建立常规工程的轻量默认，并同步简化 JOURNAL 的固定字段与罕见情况预检。
- **改动**：仅新增本条 JOURNAL 记录；未修改 profile、skills、tests 或部署状态。
- **验证 / 证据**：只读检查 portable `AGENTS.md`、`personal-project-journal`、`personal-evidence-debugging`、`personal-codex-hook-rules` 及仓库关键词命中；活动配置没有要求常规计算 SHA-256，但 JOURNAL skill 的固定六字段和多项预检会放大机械化倾向。
- **决定 / 风险**：用户明确要求禁止过度防御、主动写入哈希或 SHA-256、反复处理基本不可能的 case，以及机械执行 rubric。建议将禁令限定为 Codex 主动计算、记录或展示内容摘要；Git 内部对象标识、项目格式或协议自带字段等窄例外需避免被误伤。
- **状态 / 后续**：本轮仅讨论，尚无实施授权；如后续提出变更请求，再收敛 portable 默认与 JOURNAL skill。

## 2026-08-06T17:22:33+08:00 · high · 默认项目 JOURNAL 与文件计划协同落地

- **目标**：把 R3 的人类可审计日志能力吸纳进 portable Codex profile，同时保留 `personal-planning-with-files-zh` 对当前执行状态的独立所有权。
- **结果**：新增默认隐式 `personal-project-journal`、月度日志模板与 portable 路由；首次初始化限于已授权写任务，已有日志随后覆盖每个 substantive task；planning 历史保留改为按重要性处理。
- **改动**：修改 portable `AGENTS.md`、planning skill、manifest 与 profile contract 测试；新增 JOURNAL skill 及三份模板；为本仓库初始化 `.agent/`。除 JOURNAL 自身外未修改其他项目或远程主机。
- **验证 / 证据**：两个 skills 均通过 `quick_validate.py`；`TMPDIR=/private/tmp` 下 21 项 unit tests 全过；`git diff --check` 通过；`profile_sync.py preview` 仅显示 `AGENTS.md`、planning skill 与新增 JOURNAL skill 的预期 managed drift，未涉及 hooks 或 config。
- **决定 / 风险**：日志按月分卷、月内倒序、默认可跟踪；重要信息按审计影响保留，不按阶段年龄机械压缩。初始化时 `default_prompt` 的 `$` 曾被 shell 展开，已依据生成文件证据修正。真实隐式触发仍需部署后的 fresh task 验证。
- **状态 / 后续**：证据截止本条写入前；仓库仍基于 `cc8a9a3` 且存在本任务未提交改动。下一步按用户已授权范围审阅并提交精确路径，再执行本机 `apply`、`check` 与非强制 push。
