interface:
  display_name: "Supervise Long Jobs"
  short_description: "Persistent terminal and health event supervision"
  default_prompt: "Use $supervise-long-jobs to discover capabilities, report the expected duration, declare an observation contract, supervise this authorized long command without model polling, and report the actual duration when it ends."
dependencies:
  tools:
    - type: "mcp"
      value: "long_job_supervisor"
      description: "Discover capabilities and wait for bounded durable job events"
policy:
  allow_implicit_invocation: true
