# Source Notes

Checked: 2026-07-25.

This local workflow reflects repeated task handoffs and archive attempts. Its
stable preferences are manual invocation, a compact inline result by default,
one existing canonical file only when real continuation needs it, selective
deduplicated cross-task learning, and native task archival as the last action.

An earlier self-archive attempt interrupted reporting. The current workflow
therefore prepares the complete user-visible result before invoking the native
archive action and never emulates archival by moving runtime files.
