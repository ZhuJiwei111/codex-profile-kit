interface:
  display_name: "Monitor External Jobs"
  short_description: "Monitor any authorized long-running job"
  default_prompt: "Use $personal-monitor-external-jobs to select and prove a continuation topology, then supervise this exact authorized job read-only until terminal state or attention is required."
policy:
  allow_implicit_invocation: true
