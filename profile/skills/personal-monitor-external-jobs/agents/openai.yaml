interface:
  display_name: "Monitor External Jobs"
  short_description: "Monitor an authorized long-running external job"
  default_prompt: "Use $personal-monitor-external-jobs to monitor this exact authorized external job until it stops or needs attention."
policy:
  allow_implicit_invocation: false
