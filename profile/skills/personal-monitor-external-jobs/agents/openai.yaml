interface:
  display_name: "Monitor External Jobs"
  short_description: "Supervise one authorized external job"
  default_prompt: "Use $personal-monitor-external-jobs to establish and verify read-only monitoring for this exact authorized external job until it stops or needs attention."
policy:
  allow_implicit_invocation: true
