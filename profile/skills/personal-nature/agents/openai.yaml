interface:
  display_name: "Personal Nature Skill Router"
  short_description: "Route one request to portable Nature workflows"
  default_prompt: "Use $personal-nature to select and apply the right installed Nature workflow for this task."

policy:
  allow_implicit_invocation: false
