interface:
  display_name: "Personal Profile Sync"
  short_description: "Preview, apply, or check the portable profile"
  default_prompt: "Use $personal-profile-sync to preview the portable profile against this host."
policy:
  allow_implicit_invocation: false
