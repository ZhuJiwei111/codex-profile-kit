interface:
  display_name: "Personal Code Simplifier"
  short_description: "Simplify bounded code while preserving behavior"
  default_prompt: "Use $personal-code-simplifier to simplify this exact code surface while preserving its behavior."
policy:
  allow_implicit_invocation: false
