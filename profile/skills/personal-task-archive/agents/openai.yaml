interface:
  display_name: "Personal Task Archive"
  short_description: "Safely archive tasks on the current host"
  default_prompt: "Use $personal-task-archive to safely organize and archive eligible tasks on this task's current host."
policy:
  allow_implicit_invocation: true
