interface:
  display_name: "Personal Discussion"
  short_description: "Discuss a concern without starting implementation"
  default_prompt: "Use $personal-discussion to analyze this concern with me before any implementation."
policy:
  allow_implicit_invocation: false
