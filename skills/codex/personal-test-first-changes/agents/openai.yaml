interface:
  display_name: "Personal Test First Changes"
  short_description: "Valid RED or baseline before implementation"
  default_prompt: "Use $personal-test-first-changes to establish a valid RED or refactor baseline, make the smallest coherent change, and verify focused GREEN evidence."
policy:
  allow_implicit_invocation: true
