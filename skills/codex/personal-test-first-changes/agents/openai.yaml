interface:
  display_name: "Personal Test First Changes"
  short_description: "Establish focused behavior evidence before changes"
  default_prompt: "Use $personal-test-first-changes to establish the smallest valid before-change check, make the scoped change, and compare the same evidence afterward."
policy:
  allow_implicit_invocation: true
