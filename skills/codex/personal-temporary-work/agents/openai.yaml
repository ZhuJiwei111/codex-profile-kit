interface:
  display_name: "Temporary Work"
  short_description: "Keep one-off scripts out of production code"
  default_prompt: "Use $personal-temporary-work to handle a one-off check, statistic, or artifact post-processing task without polluting project code."
