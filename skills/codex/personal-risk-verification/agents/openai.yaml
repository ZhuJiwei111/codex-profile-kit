interface:
  display_name: "Personal Risk Verification"
  short_description: "Gate completion claims on fresh local evidence"
  default_prompt: "Use $personal-risk-verification as the final local evidence gate and report one clear conclusion with evidence, checks not run, and remaining risk."
policy:
  allow_implicit_invocation: true
