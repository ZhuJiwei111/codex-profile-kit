interface:
  display_name: "Project Output Explainer"
  short_description: "Explain current-task evidence and next step"
  default_prompt: "Use $personal-project-output-explainer with no target to explain the current task's causal snapshot and why its next step follows."

policy:
  allow_implicit_invocation: false
