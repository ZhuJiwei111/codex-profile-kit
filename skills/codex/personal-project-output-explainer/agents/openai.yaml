interface:
  display_name: "Project Output Explainer"
  short_description: "Explicitly decode the current task's output"
  default_prompt: "Use $personal-project-output-explainer to decode the current task's latest relevant output, unless I name a different exact source."

policy:
  allow_implicit_invocation: false
