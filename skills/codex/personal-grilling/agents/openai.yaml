interface:
  display_name: "Personal Grilling"
  short_description: "Pressure-test only decisions that change the plan"
  default_prompt: "Use $personal-grilling to research facts first, ask me one decision-changing question at a time in Chinese, and release planning after every blocker is locked."
policy:
  allow_implicit_invocation: false
