interface:
  display_name: "Personal Grilling"
  short_description: "Open each theme before closing every branch"
  default_prompt: "Use $personal-grilling to open each material theme with one neutral Chinese question, then narrow one decision at a time without timeout and require coverage, consistency, adversarial, risk, and confirmation closure."
policy:
  allow_implicit_invocation: false
