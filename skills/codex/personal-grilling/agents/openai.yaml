interface:
  display_name: "Personal Grilling"
  short_description: "Resolve semantic coverage one decision at a time"
  default_prompt: "Use $personal-grilling to lock a scope envelope, recommend the next choice first, resolve one material decision per round, and close with three passes plus my explicit confirmation."
policy:
  allow_implicit_invocation: false
