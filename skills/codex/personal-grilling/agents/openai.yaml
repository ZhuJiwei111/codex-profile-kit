interface:
  display_name: "Personal Grilling"
  short_description: "Close every material branch before planning"
  default_prompt: "Use $personal-grilling to build a sourced coverage tree, investigate decision-changing facts, ask me one material decision at a time in Chinese, and require coverage, consistency, adversarial, risk, and confirmation closure before handoff."
policy:
  allow_implicit_invocation: false
