interface:
  display_name: "Personal Thread Closeout"
  short_description: "Close explicit targets or sweep one project"
  default_prompt: "Use $personal-thread-closeout from this controller to process these exact target thread IDs in order, or run an explicit current-host project sweep with the strict default cutoff."
policy:
  allow_implicit_invocation: false
