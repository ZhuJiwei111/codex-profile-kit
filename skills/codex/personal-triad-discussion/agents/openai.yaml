interface:
  display_name: "Personal Triad Discussion"
  short_description: "Run a low-bandwidth GPT Pro deliberation"
  default_prompt: "Use $personal-triad-discussion to run a low-bandwidth GPT Pro Project deliberation, surface only the decisions or actions I own, and maintain one coordinator-written project memo."

policy:
  allow_implicit_invocation: false
