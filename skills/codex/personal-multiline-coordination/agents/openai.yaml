interface:
  display_name: "Worker & Worktree Coordination"
  short_description: "Coordinate visible workers and worktree integration"
  default_prompt: "Use $personal-multiline-coordination as the coordinator for visible worker tasks, isolated worktrees, event-driven intake, and authorized integration."
policy:
  allow_implicit_invocation: true
