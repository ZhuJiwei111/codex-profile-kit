interface:
  display_name: "Personal Codex Audit"
  short_description: "Audit and sync the portable Codex profile"
  default_prompt: "Use $personal-codex-audit to audit this host's reusable Codex profile or execute a safe inbound or outbound sync with codex-profile-kit."
policy:
  allow_implicit_invocation: true
