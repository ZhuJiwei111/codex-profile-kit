interface:
  display_name: "Personal Subagent Boundaries"
  short_description: "Bounded delegation and worker contracts"
  default_prompt: "Use $personal-subagent-boundaries to decide whether to delegate, select an execution profile, define exclusive worker boundaries, and intake evidence without granting final authority."
policy:
  allow_implicit_invocation: true
