interface:
  display_name: "Personal Docs Sync Light"
  short_description: "Update stale docs for identified contract changes"
  default_prompt: "Use $personal-docs-sync-light to map the identified contract delta to the smallest existing documentation surface with path-and-symbol evidence, update it, and verify affected literals, examples, links, and snippets."
