interface:
  display_name: "Personal Prompt Optimizer"
  short_description: "Repair prompts or compile zero-context handoffs"
  default_prompt: "Use $personal-prompt-optimizer to repair this prompt or compile the visible side discussion into one self-contained prompt for a fresh zero-context Codex task."
policy:
  allow_implicit_invocation: false
