interface:
  display_name: "Personal Prompt Optimizer"
  short_description: "Repair prompts or compile side-task handoffs"
  default_prompt: "Use $personal-prompt-optimizer to repair this prompt or compile the current side-task discussion into one executable handoff prompt with explicit evidence limits."

policy:
  allow_implicit_invocation: false
